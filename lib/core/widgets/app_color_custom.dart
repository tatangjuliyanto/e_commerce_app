import 'package:flutter/material.dart';

class AppColorsCustom {
  // Warna utama app (AppBar, tombol utama, dsb)
  static const Color primary = Color.fromARGB(255, 25, 170, 136);

  // Warna sekunder (opsional)
  static const Color secondary = Color(0xFF0D7377);

  // Warna background umum
  static const Color background = Color(0xFF121212);

  // Warna teks utama
  static const Color textPrimary = Colors.white;

  // Warna teks sekunder
  static const Color textSecondary = Colors.white70;

  //for red color
  static const Color red = Colors.red;

  // Warna error (untuk pesan kesalahan)
  static const Color error = Colors.redAccent;
}
